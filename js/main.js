            //Bucle do while 

/*
var tabla = 5;
var factor = 11;

        do{
            console.log (tabla + ' X ' + factor + ' = ' + (tabla * factor));
            ++factor;
        }while (factor <= 10);
*/

            //Bucle for

    var tabla = 5;

        for (let i = 0; i <= 10; i++) {
            console.log(tabla + " X " + i + " = " + tabla * i);
        }